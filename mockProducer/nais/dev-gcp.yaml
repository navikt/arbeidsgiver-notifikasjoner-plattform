apiVersion: nais.io/v1alpha1
kind: Application
metadata:
  name: mockproducer
  namespace: arbeidsgiver
  labels:
    team: arbeidsgiver
spec:
  image: {{ image }}
  team: arbeidsgiver
  liveness.path: /liveness
  readiness.path: /readiness
